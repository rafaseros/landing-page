---
import Layout from "../layouts/Layout.astro";
import "../styles/global.css";
---

<Layout>
  <main
    class="min-h-screen bg-(--bg-primary) text-(--text-primary) flex flex-col items-center justify-center p-8"
  >
    <!-- Toggle dark mode -->
    <button
      onclick="toggleTheme()"
      class="absolute top-4 right-4 p-2 rounded-lg bg-(--bg-secondary) border border-(--border) hover:bg-(--bg-tertiary) transition-colors"
    >
      <span class="dark:hidden">🌙</span>
      <span class="hidden dark:inline">☀️</span>
    </button>

    <!-- Contenido de prueba -->
    <h1 class="font-heading text-5xl font-bold mb-4">Rafael Gallegos</h1>

    <p class="font-body text-(--text-secondary) text-xl mb-8">
      Tech Lead en Healthtech
    </p>

    <button
      class="bg-accent hover:bg-accent-hover text-white font-body font-semibold px-6 py-3 rounded-lg transition-colors"
    >
      Contactar
    </button>
  </main>
</Layout>
``` --- ## Probar En tu terminal, ejecuta: ``` npm run dev
